clear; clc;

x = -4 : 0.2 : 4;
y = -4 : 0.2 : 4;

[X, Y] = meshgrid(x, y);

Z = X.^2 - Y.^2;

mesh(X, Y, Z);

title('Mesh Plot of z = x^2 - y^2');
xlabel('x axis');
ylabel('y axis');
zlabel('z axis');

grid on;
