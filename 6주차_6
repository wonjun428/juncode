clear; clc;

N = input('양의 정수 N을 입력하세요: ');

total_sum = 0;

for i = 1:N
    if (mod(i, 3) == 0) && (mod(i, 2) ~= 0)
        total_sum = total_sum + i;
    end
end

fprintf('1부터 %d까지의 수 중 3의 배수이면서 홀수인 수의 합: %d\n', N, total_sum);
